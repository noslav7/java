   version '3.1'

  services:
    db:
      image: postgres:latest
      environment:
        POSTGRES_DB: puppet_db
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: postgres
      ports:
        - 5432:5432